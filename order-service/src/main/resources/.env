PORT=8083
APPLICATION_NAME=order-service
DB_HOST=localhost
DB_PORT=5432
DB_NAME=order-service
DB_USER=postgres
DB_PASSWORD=123456

#KeyCloak Configuration (dùng chung realm với identity-service)
ISSUER_URI=http://localhost:8180/realms/identity-service

#Eureka Configuration (local dev: localhost, Docker: discovery-server)
EUREKA_SERVER_URL=http://localhost:8761/eureka
# API Gateway URL (dung cho Swagger server URL)
GATEWAY_URL=http://localhost:8888

#Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092